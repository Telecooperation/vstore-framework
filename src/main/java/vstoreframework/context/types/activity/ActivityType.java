package vstoreframework.context.types.activity;

/**
 * Possible physical activity types supported by the framework.
 * This list can be extended to support more activities.
 */
public enum ActivityType {
	STILL,
	WALKING,
	IN_VEHICLE,
	UNKNOWN
}
